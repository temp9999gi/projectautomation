#for $p in $propertyInfoList
  private ${p.propertyType} ${p.propertyName};
#end for

#for $p in $propertyInfoList
  public ${p.propertyType} get${p.capPropertyName}() {
    return ${p.propertyName};
  }
  public void set${p.capPropertyName}(String ${p.propertyName}) {
    this.${p.propertyName} = ${p.propertyName};
  }

#end for