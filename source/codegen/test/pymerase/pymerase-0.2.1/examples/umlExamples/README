simple Test Cases:  	Multiplicity		Name Labeled Ends?

author.zargo		0..* ->	 1..*		with label

automobile.zargo	none			none				//Have not tried testing

board.zargo		0..1 ->  0..1		with label			//Has problem reported bug

bookshelf.zargo		1..* ->  0..1		with label

car.zargo		1    ->  0..*		without label

computer.zargo		1..* ->  1		with label

house.zargo		1    ->  1		without label 			//Has problem reported bug

shoe.zargo		1    ->  0..1		with label			//Had problem




Complex Cases:		Multiplicity		Name Labeled Ends?

animal.zargo		1..* ->  0..*		with label			//Has typing error

animal2.zargo		1..* ->  0..*		with label

estrogen.zargo					with label